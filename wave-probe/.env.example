# Copy to .env and fill in locally. NEVER commit .env, never paste the token into chat.
# Wave developer portal -> Manage Applications -> create an application -> "Create token" (full access).
WAVE_TOKEN=
