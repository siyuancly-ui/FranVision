# Copy to `.dev.vars` for `wrangler dev` (gitignored). In production these
# are set with `wrangler secret put <NAME>` instead -- never commit real values.

DROPBOX_APP_KEY=
DROPBOX_APP_SECRET=
DROPBOX_REFRESH_TOKEN=

SUPABASE_URL=https://papaswihicvajzcubbri.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Bearer token that protects POST /admin/* . Make it long and random.
ADMIN_TOKEN=

# Bearer token for GET /render/<jobId>/<photoId> (the 2048 renders the Feature
# Sheet Builder's PDF export pulls). Use the SAME value as the FSB's admin token
# (the one typed as ?admin=<token>), because the FSB sends it as this bearer.
RENDER_TOKEN=

# Cloudflare Stream (video hosting). CF_ACCOUNT_ID is not sensitive (also set
# as a `vars` entry in wrangler.jsonc for prod) but listed here too for local
# runs. CF_STREAM_API_TOKEN needs Stream:Edit permission on that account.
CF_ACCOUNT_ID=6f3c289ac50b8015fed5082696db622b
CF_STREAM_API_TOKEN=
